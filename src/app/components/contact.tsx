'use client'
import React from 'react';
import { Title } from './title';

interface PropTypes { }

export const Contact: React.FC<PropTypes> = ({ }) => {
  return (
    <>
      <Title title={'Contact'}/>
      contact container
    </>
  )
}
